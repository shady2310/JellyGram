import React from 'react'

const MobilePostExp = (info) => {
    const noExpandida = info.NoFotoExpandida;
    
    return (
        <div>
            holaaaaaa
            <button onClick={noExpandida}>TEST</button>
        </div>
    )
}

export default MobilePostExp
